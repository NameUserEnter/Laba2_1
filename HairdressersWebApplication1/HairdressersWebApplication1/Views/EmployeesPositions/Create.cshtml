@model HairdressersWebApplication1.EmployeesPosition

@{
    ViewData["Title"] = "Create";
}

<h1>Створити</h1>

<h4>Позиція</h4>
<hr />
<div class="text-danger">@ViewBag.F</div>
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
@*            <div class="form-group">
                <label asp-for="PositionId" class="control-label"></label>
                <select asp-for="PositionId" class ="form-control" asp-items="ViewBag.PositionId">
                    <option selected="selected" disabled="disabled">Оберіть позицію</option>
                </select>
            </div>*@
            <input type = "hidden" value = "@ViewBag.PositionIdd" name = "PositionId" />
            <div class="form-group">
                <label asp-for="EmployeeId" class="control-label"></label>
                <select asp-for="EmployeeId" class ="form-control" asp-items="ViewBag.EmployeeId">
                    <option selected="selected" disabled="disabled">Оберіть працівника</option>
                </select>
            </div>
            <div class="form-group">
                <input type="submit" value="Створити" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index" asp-route-id = "@ViewBag.PositionIdd">Назад до списку</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
